apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts-service
  namespace: linkedin-microservices
  labels:
    app: posts-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: posts-service
  template:
    metadata:
      labels:
        app: posts-service
    spec:
      containers:
        - name: posts-service
          image: posts-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9010
              name: http
          env:
            - name: SERVER_PORT
              value: "9010"
            - name: CONFIG_SERVER_URL
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: CONFIG_SERVER_URL
            - name: EUREKA_SERVER_URL
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: EUREKA_SERVER_URL
            - name: EUREKA_INSTANCE_HOSTNAME
              value: "posts-service"
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: linkedin-secrets
                  key: POSTGRES_POSTS_DB_URL
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: linkedin-secrets
                  key: POSTGRES_POSTS_DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: linkedin-secrets
                  key: POSTGRES_POSTS_DB_PASSWORD
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: ZIPKIN_URL
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: ZIPKIN_URL
            - name: LOGSTASH_HOST
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: LOGSTASH_HOST
            - name: LOGSTASH_PORT
              valueFrom:
                configMapKeyRef:
                  name: linkedin-shared-config
                  key: LOGSTASH_PORT
          resources:
            requests:
              memory: "256Mi"
              cpu: "150m"
            limits:
              memory: "512Mi"
              cpu: "750m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9010
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9010
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: posts-service
  namespace: linkedin-microservices
spec:
  type: ClusterIP
  selector:
    app: posts-service
  ports:
    - name: http
      port: 9010
      targetPort: 9010
